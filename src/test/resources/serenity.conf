webdriver {
  protected driver = chrome
}

headless.mode = false

serenity {
  project.name = "PayBright Report"
  take.screenshots = AFTER_EACH_STEP
  test.root = "payBright.automation"
  tag.failures = "true"
  linked.tags = "issue"
  logging = "NORMAL"
}